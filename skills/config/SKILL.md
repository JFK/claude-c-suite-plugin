---
name: claude-c-suite-config
description: Use when configuring Claude C-Suite output language or inspecting effective local C-Suite settings.
---

# Claude C-Suite Config

Read `../../.codex-plugin/codex-adapter.md`, then read `../../commands/config.md` and
follow it as the authoritative config prompt for this Codex skill.

For Codex users, prefer `~/.codex/claude-c-suite.json` for new configuration
unless the user explicitly asks for Claude Code compatibility. Reading or
writing user-home configuration may require approval.
